trigger ProjectTaskTrigger on Project__c (after insert) {
/*
    if(trigger.isafter)
    {
        if(trigger.isinsert)
        {
            ProjectTaskClass.isMapping(trigger.new);
        }
    }
*/
}